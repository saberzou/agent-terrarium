# 2026-02-13

## Movie Oracle — Image & Layout Polish

- Enlarged cinema logo from 220px → 320px (mobile 160px → 240px)
- Swapped cinema image to higher-quality version (white bg removed via Pillow threshold 230)
- Cropped transparent padding from cinema.png (final: 614×512)
- Investigated persistent ghost rectangle around landing screen — exhaustive CSS inspection (no border/outline/shadow found), checked grid overlay, animation compositing layers, extension injection — concluded it's a headless browser screenshot artifact, not visible in real browser
- Pushed commit `711d0df` to `saberzou/movie-oracle` main
- Also committed `cinema-new.jpg` (source image) — could be cleaned up later along with other raw JPGs

### Lesson Learned
- Headless CDP screenshots can produce faint compositing artifacts around element boundaries, especially with transparent PNGs over dark backgrounds with grid textures. Don't chase rendering ghosts — verify on actual deployed site instead.

## Astra Bot Setup
- Created 3rd agent "Astra" (scholar personality, history/economics) on MiniMax M2.5
- Telegram bot: @AstraZouBot, token starts with 8062344346
- Workspace: /Users/saberzou/.openclaw/workspace-astro/
- Agent ID: astro, agentDir: /Users/saberzou/.openclaw/agents/astro/agent
- **LESSON LEARNED:** Adding a new Telegram bot account via config.patch (hot reload) crashed the gateway. New Telegram accounts need a full `openclaw gateway stop && start`, not just SIGUSR1 hot reload.
- Astra not yet functional — deferred for later. Gateway polling for the new bot never started.
